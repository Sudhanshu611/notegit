{
  "name": "notegit",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"node backend/server.js\" \"npm --prefix frontend run dev\"",
    "build": "npm --prefix frontend run build"
  },
  "devDependencies": {
    "concurrently": "^8.0.1"
  }
}
